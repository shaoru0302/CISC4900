# Product images

Put your image files here so they show on the site:

- **Necklaces (Necklace.html):**  
  - `N1.jpg` … `N12.jpg` in this folder (`images/`)  
  - `N4.jpg` in the `necklaces/` subfolder (`images/necklaces/N4.jpg`)

Paths in the HTML are already set to these locations. After adding the files, refresh the page to see them.
